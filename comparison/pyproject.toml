[project]
name = "blwarm-comparison"
version = "0.1.0"
description = "Side-by-side viewer: legacy 3-model ensemble vs bl-warm"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "jinja2>=3.1",
]
