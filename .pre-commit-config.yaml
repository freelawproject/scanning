# See https://pre-commit.com for more information
# See https://pre-commit.com/hooks.html for more hooks
exclude: migrations
default_language_version:
  python: "python3.13"
ci:
  autoupdate_schedule: quarterly
repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
     - id: check-added-large-files
       # scanning/runpod/uv.lock is intentionally large (locked GPU
       # worker deps: torch cu126 + paddlepaddle-gpu + blackletter and
       # their transitives). Reproducible builds require tracking it.
       exclude: ^scanning/runpod/uv\.lock$
     - id: check-ast
     - id: check-json
     - id: check-merge-conflict
     - id: check-toml
     - id: check-xml
     - id: check-yaml
     - id: debug-statements
     - id: detect-private-key
     - id: fix-byte-order-marker
     - id: trailing-whitespace
       args: [--markdown-linebreak-ext=md]

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.14.11
    hooks:
      - id: ruff
        args: [ --fix ]
      - id: ruff-format
